<?xml version="1.0" encoding="utf-8" ?>
<sqlMap
	namespace="IKaan.Was.Core.SqlMaps"
	xmlns="http://ibatis.apache.org/mapping"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" >

	<statements>
		<select id="SelectAAGroupList" parameterClass="DataMap" resultClass="AAGroup" >
			<![CDATA[
				WITH CTE (ID, HierID, Level)
				AS
				(
					SELECT	AA.ID		as ID
						,	CAST(RIGHT('0000' + 
									CONVERT(VARCHAR, ROW_NUMBER() OVER (ORDER BY AA.ID)), 4) AS VARCHAR(MAX))	AS HierID
						,	1																					AS Level
					FROM	dbo.AAGroup AA WITH (NOLOCK)
					WHERE	1=1
			]]>
			<isNotEmpty property="ParentID">
				<![CDATA[
					And	AA.ID = #ParentID#
				]]>
			</isNotEmpty>
			<isEmpty property="ParentID">
				<![CDATA[
					AND	AA.ParentID IS NULL
				]]>
			</isEmpty>
			<![CDATA[
					UNION ALL
					SELECT	AA.ID			as ID
						,	CAST(BB.HierID + 
								 RIGHT('0000' + CONVERT(VARCHAR, ROW_NUMBER() OVER (ORDER BY AA.ID)), 4) AS VARCHAR(MAX))	AS HierID
						,	BB.Level + 1																					AS Level
					FROM	dbo.AAGroup AA WITH (NOLOCK)
							INNER JOIN 
								CTE BB 
									ON AA.ParentID = BB.ID
				)
				SELECT  ROW_NUMBER() OVER (ORDER BY BB.HierID)	AS RowNo
					,	AA.ID
					,	AA.CreateDate
					,	AA.CreateBy
					,	AA.CreateByName
					,	AA.UpdateDate
					,	AA.UpdateBy
					,	AA.UpdateByName
					
					,	AA.GroupName
					,	AA.ParentID
					,	AA.UseYn
					,	AA.Description
					
					,	BB.HierID
					,	REPLICATE(' ', (BB.Level -1) * 5) + 
						CASE 
							WHEN BB.Level > 1 THEN '▶'
							ELSE ''
						END + 
						AA.GroupName		AS HierName
					,	BB.Level			as GroupLevel
					
				FROM	dbo.AAGroup AA WITH (NOLOCK)
						INNER JOIN 
							CTE BB 
								ON AA.ID = BB.ID
				WHERE	1=1				
			]]>
			<isNotEmpty property="FindText">
				<![CDATA[
					AND	AA.GroupName LIKE '%' + #FindText# + '%'
				]]>
			</isNotEmpty>
			<![CDATA[
				ORDER BY RowNo
			]]>
		</select>
		<select id="SelectAAGroup" parameterClass="DataMap" resultClass="AAGroup" >
			<![CDATA[
				SELECT  AA.ID
					,	AA.CreateDate
					,	AA.CreateBy
					,	AA.CreateByName
					,	AA.UpdateDate
					,	AA.UpdateBy
					,	AA.UpdateByName
					,	AA.GroupName
					,	AA.ParentID
					,	AA.UseYn
					,	AA.Description
				FROM	dbo.AAGroup AA WITH (NOLOCK)
				WHERE	AA.ID = #ID#
			]]>
		</select>
		<insert id="InsertAAGroup" parameterClass="AAGroup" >
			<![CDATA[
				INSERT INTO dbo.AAGroup
				( 
					CreateDate, CreateBy, CreateByName,
					GroupName, ParentID, UseYn, Description
				)
				VALUES
				(
					GetDate(), #CreateBy#, #CreateByName#,
					#GroupName#, #ParentID#, #UseYn#, #Description#
				)
			]]>
			<selectKey type="post" property="ID" resultClass="Int">
				<![CDATA[
					SELECT @@IDENTITY AS ID
				]]>
			</selectKey>
		</insert>
		<update id="UpdateAAGroup" parameterClass="AAGroup">
			<![CDATA[
				UPDATE  dbo.AAGroup
				SET     UpdateDate	= GetDate()
					,	UpdateBy	= #UpdateBy#
					,	UpdateByName= #UpdateByName#
					
					,	GroupName	= #GroupName#
					,	ParentID	= #ParentID#
					,	UseYn		= #UseYn#
					,	Description	= #Description#
				WHERE	ID			= #ID#
			]]>
		</update>
		<delete id="DeleteAAGroup" parameterClass="DataMap">
			<![CDATA[
				DELETE FROM dbo.AAGroupMenu WHERE GroupID = #ID#;
				DELETE FROM dbo.AAGroupRole WHERE GroupID = #ID#;
				DELETE FROM dbo.AAGroup 	WHERE ID = #ID#;
			]]>
		</delete>

		<select id="SelectAAGroupRoleList" parameterClass="DataMap" resultClass="AAGroupRole" >
			<![CDATA[
				WITH CTE (ID, HierID, Level)
				AS
				(
					SELECT	AA.ID		as ID
						,	CAST(RIGHT('0000' + 
									CONVERT(VARCHAR, 
										ROW_NUMBER() OVER 
											(ORDER BY AA.ID)), 4) AS VARCHAR(MAX))	as HierID
						,	1														as Level
					FROM	dbo.AARole AA WITH (NOLOCK)
					WHERE	AA.ParentID IS NULL
					AND		AA.UseYn = 'Y'
					UNION ALL
					SELECT	AA.ID			as ID
						,	CAST(BB.HierID + 
								 RIGHT('0000' + 
									CONVERT(VARCHAR, 
										ROW_NUMBER() OVER 
											(ORDER BY AA.ID)), 4) AS VARCHAR(MAX))	as HierID
						,	BB.Level + 1											as Level
					FROM	dbo.AARole AA WITH (NOLOCK)
							INNER JOIN 
								CTE BB 
									ON AA.ParentID = BB.ID
					WHERE	AA.UseYn = 'Y'
				)
				SELECT  ROW_NUMBER() OVER (ORDER BY AA.HierID)	AS RowNo
					,	CASE 
							WHEN GR.ID IS NULL THEN 'N'
							ELSE GR.UseYn
						END										AS Checked
					,	GR.ID
					,	GR.CreateDate
					,	GR.CreateBy
					,	GR.CreateByName
					,	GR.UpdateDate
					,	GR.UpdateBy
					,	GR.UpdateByName
					
					,	#ID#					as GroupID
					,	AA.ID					as RoleID
					,	ISNULL(GR.UseYn,'N')	as UseYn
					,	BB.RoleName				as RoleName
					
					,	AA.HierID
					,	REPLICATE(' ', (AA.Level -1) * 5) + 
						CASE 
							WHEN AA.Level > 1 THEN '▶'
							ELSE ''
						END + 
						BB.RoleName			AS HierName
					,	AA.Level			as RoleLevel
					
				FROM	CTE AA
						INNER JOIN
							dbo.AARole BB WITH (NOLOCK)
								ON AA.ID = BB.ID
						LEFT JOIN
							dbo.AAGroupRole GR WITH (NOLOCK)
								ON AA.ID = GR.RoleID
									AND GR.GroupID = #ID#
				ORDER BY RowNo
			]]>
		</select>
		<select id="SelectAAGroupMenuList" parameterClass="DataMap" resultClass="AAGroupMenu" >
			<![CDATA[
				WITH CTE (ID, HierID, Level)
				AS
				(
					SELECT	AA.ID		as ID
						,	CAST(RIGHT('0000' + 
									CONVERT(VARCHAR, 
										ROW_NUMBER() OVER 
											(ORDER BY AA.SortOrder, AA.ID)), 4) AS VARCHAR(MAX))	as HierID
						,	1																		as Level
					FROM	dbo.AAMenu AA WITH (NOLOCK)
					WHERE	AA.ParentID IS NULL
					AND		AA.UseYn = 'Y'
					UNION ALL
					SELECT	AA.ID			as ID
						,	CAST(BB.HierID + 
								 RIGHT('0000' + 
									CONVERT(VARCHAR, 
										ROW_NUMBER() OVER 
											(ORDER BY AA.SortOrder, AA.ID)), 4) AS VARCHAR(MAX))	as HierID
						,	BB.Level + 1															as Level
					FROM	dbo.AAMenu AA WITH (NOLOCK)
							INNER JOIN 
								CTE BB 
									ON AA.ParentID = BB.ID
					WHERE	AA.UseYn = 'Y'
				)
				SELECT  ROW_NUMBER() OVER (ORDER BY AA.HierID)	AS RowNo
					,	CASE 
							WHEN GM.ID IS NULL THEN 'N'
							ELSE GM.UseYn
						END										AS Checked
					,	GM.ID
					,	GM.CreateDate
					,	GM.CreateBy
					,	GM.CreateByName
					,	GM.UpdateDate
					,	GM.UpdateBy
					,	GM.UpdateByName
					
					,	#ID#					as GroupID
					,	AA.ID					as MenuID
					,	ISNULL(GM.UseYn,'N')	as UseYn
					,	BB.MenuName				as MenuName
					
					,	AA.HierID				as HierID
					,	REPLICATE(' ', (AA.Level -1) * 5) + 
						CASE 
							WHEN AA.Level > 1 THEN '▶'
							ELSE ''
						END + 
						BB.MenuName				as HierName
					,	AA.Level				as MenuLevel
					
				FROM	CTE AA
						INNER JOIN
							dbo.AAMenu BB WITH (NOLOCK)
								ON AA.ID = BB.ID
						LEFT JOIN
							dbo.AAGroupMenu GM WITH (NOLOCK)
								ON AA.ID = GM.MenuID
									AND GM.GroupID = #ID#
				ORDER BY RowNo
			]]>
		</select>
	</statements>
</sqlMap>